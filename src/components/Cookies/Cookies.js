// import Cookies from "js-cookie";

// export const setSellerLoginCookie = () => {
//   Cookies.set("sellerIsLoggedin", "true", { expires: 1 }); // Expires in 1 day
// };

// export const setUserLoginCookie = () => {
//   Cookies.set("userIsLoggedin", "true", { expires: 1 }); // Expires in 1 day
// };

// export const removeSellerLoginCookie = () => {
//   Cookies.remove("sellerIsLoggedin");
// };

// export const removeUserLoginCookie = () => {
//   Cookies.remove("userIsLoggedin");
// };
